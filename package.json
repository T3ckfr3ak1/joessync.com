{
  "name": "joessync-website",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev public --port 3000",
    "deploy": "wrangler pages deploy public --project-name joessync-website"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
